Интерфейс Queue расширяет интерфейс Collection и определяет поведение очереди,
    работающей по принципу "первым вошел - первым обслужен" (FIFO). Очереди
    могут быть организованы на основе различных критериев. Интерфейс Queue
    является обобщенным и объявляется следующим образом:
        - interface Queue<E>
            где E обозначает тип объектов, хранимых в очереди.

    Основные методы интерфейса Queue:
            E element()
            Описание: Возвращает элемент из головы очереди, не удаляя его. Генерирует
            исключение NoSuchElementException, если очередь пуста.

            boolean offer(E e)
            Описание: Пытается добавить элемент в очередь. Возвращает true при успешном
            добавлении, false при неудаче (например, если очередь фиксированной длины
            заполнена).

            E peek()
            Описание: Возвращает элемент из головы очереди, не удаляя его. Возвращает
            null, если очередь пуста.

            E poll()
            Описание: Возвращает и удаляет элемент из головы очереди. Возвращает null,
            если очередь пуста.

            E remove()
            Описание: Удаляет и возвращает элемент из головы очереди. Генерирует
            исключение NoSuchElementException, если очередь пуста.

    Исключения, генерируемые методами Queue:
        ClassCastException: Если объект несовместим с элементами очереди.
        NullPointerException: Если предпринимается попытка добавить null,
            а очередь не допускает null элементы.
        IllegalArgumentException: Если передан неверный аргумент.
        IllegalStateException: Если очередь фиксированной длины заполнена
            и добавление нового элемента невозможно.
        NoSuchElementException: Если предпринимается попытка удалить элемент
        из пустой очереди.

    Важные аспекты:
        - Удаление элементов: Элементы могут удаляться только из начала очереди.
        - Различие между poll() и remove():
            poll(): Возвращает null, если очередь пуста.
            remove(): Генерирует NoSuchElementException, если очередь пуста.

        - Различие между element() и peek():
            element(): Генерирует NoSuchElementException, если очередь пуста.
            peek(): Возвращает null, если очередь пуста.

        - Метод offer(): Пытается добавить элемент в очередь, но может
            завершиться неудачно, если очередь фиксированной длины заполнена.