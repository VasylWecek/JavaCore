Интерфейс SortedMap расширяет интерфейс Map и обеспечивает упорядоченное размещение записей по ключам
    в порядке возрастания.

    interface SortedMap<K, V>

    Comparator <? super K> comparator() - Возвращает компаратор, используемый для упорядочения элементов
        в отображении. Если используется естественное упорядочение, возвращает null.
    K firstKey() - Возвращает первый (наименьший) ключ в отображении.
    SortedMap<K, V> headMap(K end) - Возвращает отображение, содержащее элементы от начало (включительно)
        до конец (исключительно).
    SortedMap<K, V> tailMap(K start) - Возвращает отображение, содержащее элементы, начиная с указанного ключа начало (включительно).

    Исключения, генерируемые методами интерфейса SortedMap
    NoSuchElementException: если вызывающее отображение пусто.
    ClassCastException: если заданный объект несовместим с элементами отображения.
    NullPointerException: если используется пустой объект, когда пустые объекты не допускаются.
    IllegalArgumentException: если указан неверный аргумент.

Интерфейс SortedMap обеспечивает упорядоченное хранение элементов по ключам в порядке возрастания и
    предоставляет методы для работы с подотображениями. Это делает его полезным для задач, где необходимо
    поддерживать упорядоченность элементов, а также эффективно манипулировать подмножествами отображения.