Класс IdentityHashMap расширяет класс AbstractMap и реализует интерфейс Map. Основное отличие
    IdentityHashMap от HashMap заключается в том, что для сравнения ключей и значений используется
    проверка их идентичности, то есть сравнение по ссылкам (==), а не метод equals().

    class IdentityHashMap<K, V>
    Особенности:
        Сравнение по идентичности:
            - При сравнении ключей и значений используется оператор ==, что означает сравнение по ссылкам.
            - Это отличает IdentityHashMap от других реализаций Map, которые используют метод equals() для сравнения.

        Обобщенность:
            - Класс IdentityHashMap является обобщенным, что позволяет ему работать с любыми типами ключей и значений, указанных в параметрах типа.

        Использование:
            - В документации API указано, что IdentityHashMap не предназначен для общего применения.
            Он используется в специфических сценариях, где требуется сравнение по идентичности.


    Конструкторы
        - IdentityHashMap()
Создает пустое отображение с начальной емкостью по умолчанию.

        - IdentityHashMap(int initialCapacity)
Создает пустое отображение с указанной начальной емкостью.

        - IdentityHashMap(Map<? extends K, ? extends V> m)
Создает отображение, инициализированное элементами из заданного отображения m.

        - IdentityHashMap(int expectedMaxSize, float loadFactor)
Создает пустое отображение с указанной начальной емкостью и коэффициентом загрузки.





