Класс HashMap расширяет AbstractMap и реализует интерфейс Map. Он использует
    хеш-таблицу для хранения
    отображения, обеспечивая постоянное время выполнения методов get() и put(),
        даже для крупных отображений.

    class HashMap<K, V>

    Конструкторы класса HashMap:
        - HashMap():
            Создает пустое хеш-отображение по умолчанию.
        - HashMap(Map<? extends K, ? extends V> m):
            Создает хеш-отображение, инициализированное элементами заданного отображения m.
        - HashMap(int емкость):
            Создает хеш-отображение с заданной начальной емкостью.
        - HashMap(int емкость, float коэффициент_заполнения)
            Создает хеш-отображение с заданной емкостью и коэффициентом заполнения.
            По умолчанию емкость составляет 16, а коэффициент заполнения - 0.75.

    Характеристики HashMap
        - Не гарантируется порядок расположения элементов.
        - Итератор может извлекать элементы в произвольном порядке, который не обязательно соответствует
            порядку ввода.

    Примечание о сложности
    В лучшем случае, операции добавления, поиска и удаления выполняются за
        константное время O(1).
    В худшем случае, когда все элементы попадают в одну корзину, временная
        сложность операций может достигать O(n).

    В HashMap происходит динамическое изменение размера массива корзин (buckets)
        для поддержания эффективности операций добавления, удаления и поиска.
        Этот процесс называется ресайзингом (resizing) и происходит при достижении
        определённого порога заполненности, называемого коэффициентом загрузки
        (load factor).

    По умолчанию коэффициент загрузки равен 0.75, что означает, что при заполнении
        75% корзин происходит увеличение их количества.